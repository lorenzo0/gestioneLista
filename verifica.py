
#def verifica():
    if(os.path.isfile('lista.lst'))
        #controlla gli atlri
        if(os.path.exists(error))
            
        else
            #crea la cartella error
            mk_index(error)
        if(os.path.exists(ok))
            
        else
            #crea la cartella ok
            mk_index(ok)
    else
        #stop

#pass